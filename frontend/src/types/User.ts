export type User = {
	token: string;
	email: string;
	name: string;
};

export type SigninUser = {
	email: string;
	password: string;
};

export type SignupUser = {
	email: string;
	password: string;
	name: string;
};
